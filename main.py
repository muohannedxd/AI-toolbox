import tkinter as tk
from mainGui import mainGUI

# Create the main window for the Graph GUI
root = tk.Tk()

# Initialize the Graph GUI
graph_gui = mainGUI(root)

# Start the Tkinter event loop
root.mainloop()
